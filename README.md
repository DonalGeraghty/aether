# Aether

A barebones home workout tracker built with React and Vite. The initial three-day program is based on the plan in the neighbouring Achilles project.

## Run locally

```bash
npm install
npm run dev
```

Workout drafts and completed sessions are stored in the browser with `localStorage`; there is no account or backend yet.
